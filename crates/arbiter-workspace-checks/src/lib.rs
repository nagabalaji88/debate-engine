//! Empty on purpose. Every check this crate contributes is an integration test in
//! `tests/`, run against `cargo metadata` for the whole workspace — there is no
//! library surface to test in isolation.
#![forbid(unsafe_code)]
